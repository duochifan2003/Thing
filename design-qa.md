# 小组件设计 QA

- source visual truth: `/tmp/event-atlas-app-reference.png`
- implementation screenshot: `/tmp/event-atlas-widget-redesign-three-rendered.png`
- full comparison: `/tmp/event-atlas-design-comparison-six.png`
- focused comparison: `/tmp/event-atlas-design-focus-comparison-six.png`
- viewport: macOS 中尺寸小组件，344 × 164 pt
- state: 展示最近三条事件；每条均显示完整日期与标题

## 尺寸与归一化

- 应用参考图：1120 × 739 px，是应用内时间线的色彩与字重参考。
- 实现截图：688 × 328 px，由 `EventAtlasWidgetView` 在 344 × 164 pt、2x 密度下渲染。
- 全景对照图：4480 × 1478 px；在同一米白画布中并列应用与小组件。
- 聚焦对照图：2496 × 656 px；左侧为 560 × 328 px 的应用时间线裁切，右侧为 688 × 328 px 的小组件。两者窗口类型不同，因此只对齐视觉语言和内容层级，不把桌面窗口比例当作组件规格。

## 全景对照

- 字体与层级：主标题为 14 pt semibold；三条事件标题均为 13 pt medium，日期胶囊使用 9 pt 深绿色等宽字。
- 间距与布局：中尺寸关闭系统内容边距，改用左右 18 pt、上下 14 pt 的明确网格。标题行固定 20 pt，标题至首条的布局间距为 5 pt，视觉间距约 10–12 pt；事件行之间保持 14 pt。日期列与正文列间距为 12 pt。三条事件均为 27 pt 高。
- 颜色与标记：沿用米白背景、深绿文字和浅绿色日期块；日期块固定为 68 × 18 pt，日期文字居中。中尺寸已移除原有珊瑚圆点、竖线和首条底色。
- 图片与资产：该组件没有图片内容，也没有新增或仿制图片资产。
- 文案与内容：保留“3 条”和事件原文；每条左侧日期胶囊显示完整年月日。共享数据尚无独立时分字段，因此不填充虚构时间。

## 聚焦对照

聚焦图显示首条直接铺在米白背景上，以标题与地点的层级承担重点；后两条保持轻量列表行，日期色块提供可扫读的时间锚点。

## 比较历史

1. 前一版将三条记录放进一张白色大卡片，使用横向分割线、珊瑚圆点与浅色竖线。
2. 本轮按反馈移除大卡片和分割线，日期改为色块。
3. 最后移除首条浅色底块，并改为 18 × 14 pt 的外层网格。
4. 年份插入左侧日期胶囊，改为完整年月日；全景与聚焦对照复核后，新的层级和间距未发现 P0、P1 或 P2 问题。
5. 移除首条专用的地点副标题，三条记录改为相同层级。

## Findings

无阻断或中等级别视觉问题。

## Final result

final result: passed
